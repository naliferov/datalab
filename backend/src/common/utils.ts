import { ulid } from 'ulid';
import { DataType } from '../modules/data-editor/entities/data.type';

export const makeUlid = () => ulid();

export const serializeData = (data: DataType) => {};
export const unserializeData = () => {};
